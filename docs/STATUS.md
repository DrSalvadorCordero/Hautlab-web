# Status

Canonical production project: Vercel `hautlabmx.com` (`prj_zfDNkaKDMfEzYnShw6qXetYkOrgD`).
Git source of truth: `DrSalvadorCordero/Hautlab-web` branch `main`.

Paused clones (2026-09-07, Grok): `hautlab-web`, `hautlabmx`, `hautlab-deploy-probe`.
Not paused: `hautlab-wa-command-center` — same GitHub repo, report only; waiting on Salvador.

Clerk debug endpoint and sign-in repair logic were removed on `main` (`918e8f27`, `5159c0b7`). See `docs/CLERK_CLEANUP.md` and issue #91.

Rule: no direct pushes to `main` for Vercel/Clerk/web unless Salvador authorizes a hotfix.
